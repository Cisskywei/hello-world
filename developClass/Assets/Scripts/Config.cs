using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Config  {

	public enum SyncTransform
    {
        POSITION = 1,
        ROTATION,
        SCALE,
    }

    public enum Patterns
    {
        CLASSROOMINTERACTION = 1,
        VRINTERACTION,
        BROADCASTINGMODE,
    }
}
